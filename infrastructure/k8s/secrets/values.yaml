# K8s Secrets 配置参考
# 用于生产环境敏感信息的安全管理
# 所有密钥必须通过 Kubernetes Secret 或 Vault 外部注入

# ============================================================
# 数据库密钥
# ============================================================
# kubectl create secret generic db-secrets \
#   --from-literal=username=edams_user \
#   --from-literal=password=$(openssl rand -base64 32) \
#   --namespace=edams

# ============================================================
# JWT 密钥
# ============================================================
# kubectl create secret generic jwt-secrets \
#   --from-literal=secret=$(openssl rand -base64 64) \
#   --namespace=edams

# ============================================================
# 数据源加密密钥 (P0安全修复)
# ============================================================
# kubectl create secret generic datasource-secrets \
#   --from-literal=encryption-key=$(openssl rand -base64 32) \
#   --namespace=edams

# ============================================================
# Redis 密钥
# ============================================================
# kubectl create secret generic redis-secrets \
#   --from-literal=password=$(openssl rand -base64 32) \
#   --namespace=edams

# ============================================================
# Nacos 配置参考
# ============================================================
# 配置中心敏感配置（application-prod.yml）
# spring:
#   datasource:
#     password: ${DB_PASSWORD}
#   data:
#     redis:
#       password: ${REDIS_PASSWORD}
# jwt:
#   secret: ${JWT_SECRET}
# datasource:
#   encryption:
#     key: ${DATASOURCE_ENCRYPTION_KEY}
