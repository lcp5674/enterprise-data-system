# 企业数据资产管理系统 - 测试任务分配方案

**文档版本**：V1.0
**编制日期**：2026年4月10日
**编制人**：测试架构师

---

## 1. 测试任务分配概览

### 1.1 测试工程师团队

| 工程师 | 职责 | 负责范围 |
|--------|------|----------|
| test-engineer-unit | 单元测试工程师 | 41个微服务的单元测试，目标覆盖率>70% |
| test-engineer-integration | 集成测试工程师 | 服务间通信、数据库、中间件集成测试 |
| test-engineer-e2e | 端到端测试工程师 | 核心业务流程E2E测试 |

### 1.2 微服务分类矩阵

| 优先级 | 领域 | 服务列表 | Service类数量 | Controller类数量 |
|--------|------|----------|---------------|------------------|
| P0 | 认证与权限 | edams-auth, edams-permission | 8 | 4 |
| P0 | 资产管理 | edams-asset | 核心模块 | 核心模块 |
| P0 | 数据血缘 | lineage-service | 1 | 1 |
| P0 | 质量检测 | quality-service | 2 | 2 |
| P1 | 数据发现 | catalog, datamap, datasource, search | 7 | 4 |
| P1 | 知识图谱 | edams-knowledge | 4 | 4 |
| P1 | 数据治理 | governance-engine, standard-service | 6 | 6 |
| P2 | 高级功能 | LLM, chatbot, workflow等 | 15 | 10 |
| P2 | 运营支撑 | notification, lifecycle, collaboration | 12 | 8 |

---

## 2. 单元测试任务分配

### 2.1 test-engineer-unit 任务清单

#### 阶段1：认证与权限域（P0，1-2天）

**edams-auth 模块**
```
任务编号: UNIT-AUTH-001
服务: edams-auth
测试范围:
  - AuthService: login, verifyMFA, logout, refreshToken, validateToken
  - TokenService: token生成/验证/刷新
  - CaptchaService: 验证码生成/验证
  - MFAService: MFA设置/启用/禁用/备用码验证
  - LoginSecurityService: 登录安全检查
覆盖率目标: 90%
```

**edams-permission 模块**
```
任务编号: UNIT-PERM-001
服务: edams-permission
测试范围:
  - PermissionService: 权限检查/授予/撤销
  - RoleService: 角色CRUD/权限分配
  - MenuService: 菜单权限控制
覆盖率目标: 90%
```

#### 阶段2：核心业务域（P0，3-5天）

**lineage-service**
```
任务编号: UNIT-LINEAGE-001
服务: lineage-service
测试范围:
  - LineageService.createLineage: 创建血缘关系
  - LineageService.deleteLineage: 删除血缘关系
  - LineageService.queryLineageGraph: 查询血缘图谱
  - LineageService.analyzeImpact: 影响分析
  - LineageService.traceLineage: 溯源分析
  - LineageService.parseSql: SQL解析
Mock依赖: LineageGraphRepository, LineageRelationRepository
覆盖率目标: 85%
```

**quality-service**
```
任务编号: UNIT-QUALITY-001
服务: quality-service
测试范围:
  - QualityCheckService.executeCheck: 执行检查
  - QualityCheckService.executeCheckAsync: 异步检查
  - QualityCheckService.batchExecuteCheck: 批量检查
  - QualityRuleService: 规则管理
Mock依赖: KafkaTemplate, QualityCheckTaskRepository
覆盖率目标: 85%
```

**standard-service**
```
任务编号: UNIT-STANDARD-001
服务: standard-service
测试范围:
  - DataStandardService: 数据标准管理
  - ComplianceCheckService: 合规检查
  - StandardMappingService: 标准映射
覆盖率目标: 80%
```

**governance-engine**
```
任务编号: UNIT-GOVERNANCE-001
服务: governance-engine
测试范围:
  - GovernanceOrchestrationService: 治理编排
  - AIRecommendationService: AI推荐
覆盖率目标: 75%
```

#### 阶段3：数据发现域（P1，2-3天）

```
任务编号: UNIT-DISCOVERY-001
服务: edams-discovery/*
测试范围:
  - CatalogService: 目录管理
  - DatamapService: 数据地图
  - DatasourceConfigService: 数据源配置
  - SearchService: 搜索功能
  - ModelService: 模型管理
  - InsightService: 洞察分析
  - MetricService: 指标管理
覆盖率目标: 75%
```

#### 阶段4：知识智能域（P1，2天）

```
任务编号: UNIT-KNOWLEDGE-001
服务: edams-knowledge, edams-llm, edams-chatbot
测试范围:
  - KnowledgeGraphService: 知识图谱
  - OntologyService: 本体管理
  - GraphQueryService: 图查询
  - LlmChatService: LLM对话
  - QuotaService: 配额管理
覆盖率目标: 70%
```

#### 阶段5：支撑服务域（P2，2天）

```
任务编号: UNIT-SUPPORT-001
服务: edams-notification, edams-workflow, edams-lifecycle等
测试范围:
  - NotificationService: 通知发送
  - Workflow服务: 流程定义/实例/任务
  - Lifecycle服务: 生命周期管理
覆盖率目标: 65%
```

### 2.2 单元测试代码规范

```java
// 标准单元测试模板
@ExtendWith(MockitoExtension.class)
class XxxServiceTest {

    @Mock
    private XxxRepository xxxRepository;

    @Mock
    private ExternalService externalService;

    @InjectMocks
    private XxxService xxxService;

    @Test
    void testMethod_Scenario_ExpectedResult() {
        // Given
        when(xxxRepository.findById(1L)).thenReturn(Optional.of(entity));
        
        // When
        Result result = xxxService.method(input);
        
        // Then
        assertNotNull(result);
        assertEquals(expected, result.getField());
        verify(xxxRepository, times(1)).findById(1L);
    }

    @Test
    void testMethod_Exception_Thrown() {
        // Given
        when(xxxRepository.findById(1L)).thenReturn(Optional.empty());
        
        // When & Then
        assertThrows(CustomException.class, () -> 
            xxxService.method(input)
        );
    }
}
```

---

## 3. 集成测试任务分配

### 3.1 test-engineer-integration 任务清单

#### 阶段1：数据库集成（P0，2天）

```
任务编号: INT-DB-001
测试范围: PostgreSQL数据库集成
测试内容:
  - Repository层CRUD操作
  - 事务管理测试
  - 关联查询测试
  - 批量操作测试
工具: Spring Boot Test + Testcontainers
```

#### 阶段2：Redis缓存集成（P0，1天）

```
任务编号: INT-REDIS-001
测试范围: Redis缓存集成
测试内容:
  - 缓存读写
  - 缓存过期
  - 缓存穿透防护
  - 缓存雪崩防护
  - 分布式锁
工具: Embedded Redis / Testcontainers
```

#### 阶段3：Kafka消息集成（P0，1天）

```
任务编号: INT-KAFKA-001
测试范围: Kafka消息队列集成
测试内容:
  - 消息发送
  - 消息消费
  - 消息顺序
  - 事务消息
工具: Spring Kafka Test + Embedded Kafka
```

#### 阶段4：Neo4j图数据库集成（P0，1天）

```
任务编号: INT-NEO4J-001
测试范围: Neo4j图数据库集成
测试内容:
  - 节点创建/查询/删除
  - 关系创建/查询/删除
  - 图遍历查询
  - 路径查询
工具: Neo4j Test Driver
```

#### 阶段5：服务间API集成（P1，2天）

```
任务编号: INT-API-001
测试范围: 服务间HTTP调用
测试内容:
  - Feign Client调用
  - 熔断降级
  - 超时处理
  - 错误传播
工具: WireMock / MockServer
```

### 3.2 集成测试模板

```java
@SpringBootTest
@AutoConfigureTestDatabase(replace = AutoConfigureTestDatabase.Replace.NONE)
@Testcontainers
class XxxRepositoryIntegrationTest {

    @Container
    static PostgreSQLContainer<?> postgres = new PostgreSQLContainer<>("postgres:15")
        .withDatabaseName("test")
        .withUsername("test")
        .withPassword("test");

    @DynamicPropertySource
    static void properties(DynamicPropertyRegistry registry) {
        registry.add("spring.datasource.url", postgres::getJdbcUrl);
        registry.add("spring.datasource.username", postgres::getUsername);
        registry.add("spring.datasource.password", postgres::getPassword);
    }

    @Test
    void testRepository_CrudOperations() {
        // Integration test implementation
    }
}
```

---

## 4. 端到端测试任务分配

### 4.1 test-engineer-e2e 任务清单

#### 阶段1：认证流程E2E（P0，1天）

```
任务编号: E2E-AUTH-001
场景: 用户登录流程
测试步骤:
  1. 打开登录页面
  2. 输入用户名密码
  3. 输入验证码（如有）
  4. 点击登录
  5. 验证跳转到首页
  6. 验证用户信息展示
预期结果:
  - 登录成功，显示用户信息
  - Token正确存储
工具: Playwright / Cypress
```

#### 阶段2：资产管理E2E（P0，2天）

```
任务编号: E2E-ASSET-001
场景: 创建数据资产
测试步骤:
  1. 登录系统
  2. 进入数据地图
  3. 点击新建资产
  4. 填写资产信息（名称、类型、描述）
  5. 配置字段信息
  6. 设置分类分级
  7. 提交创建
预期结果:
  - 资产创建成功
  - 资产出现在列表中
  - 可以查看资产详情
```

```
任务编号: E2E-ASSET-002
场景: 资产搜索与筛选
测试步骤:
  1. 登录系统
  2. 进入数据地图
  3. 输入关键词搜索
  4. 使用筛选条件
  5. 分页浏览
预期结果:
  - 搜索结果正确
  - 筛选条件生效
```

#### 阶段3：血缘管理E2E（P0，2天）

```
任务编号: E2E-LINEAGE-001
场景: 创建血缘关系
测试步骤:
  1. 登录系统
  2. 进入资产详情页
  3. 点击血缘tab
  4. 创建上下游关系
  5. 填写转换说明
  6. 保存血缘关系
预期结果:
  - 血缘关系创建成功
  - 血缘图谱正确展示
```

```
任务编号: E2E-LINEAGE-002
场景: 影响分析
测试步骤:
  1. 选择资产
  2. 点击影响分析
  3. 设置分析深度
  4. 查看影响范围
预期结果:
  - 正确显示所有下游影响
  - 影响程度计算正确
```

#### 阶段4：质量检测E2E（P0，2天）

```
任务编号: E2E-QUALITY-001
场景: 执行质量检测
测试步骤:
  1. 登录系统
  2. 选择资产
  3. 配置质量规则
  4. 触发质量检测
  5. 查看检测结果
  6. 查看质量报告
预期结果:
  - 质量检测执行成功
  - 结果展示正确
  - 评分计算正确
```

#### 阶段5：关键业务流程E2E（P1，2天）

```
任务编号: E2E-BUSINESS-001
场景: 端到端数据治理流程
测试步骤:
  1. 登录系统（用户A）
  2. 创建数据资产
  3. 配置血缘关系
  4. 设置质量规则
  5. 执行质量检测
  6. 生成质量报告
  7. 分享给其他用户（用户B）
预期结果:
  - 全流程顺畅
  - 数据一致
  - 权限控制正确
```

### 4.2 E2E测试模板

```typescript
// Playwright测试模板
import { test, expect } from '@playwright/test';

test.describe('资产管理系统E2E测试', () => {
  let authToken: string;

  test.beforeAll(async ({ request }) => {
    // 登录获取Token
    const response = await request.post('/api/v1/auth/login', {
      data: {
        username: 'testuser',
        password: 'password123'
      }
    });
    authToken = (await response.json()).data.accessToken;
  });

  test('创建数据资产', async ({ page }) => {
    await page.goto('/data-map');
    await page.click('button:has-text("新建")');
    await page.fill('input[name="name"]', '测试资产');
    await page.selectOption('select[name="type"]', 'TABLE');
    await page.fill('textarea[name="description"]', '测试描述');
    await page.click('button:has-text("保存")');
    
    await expect(page.locator('.toast')).toContainText('创建成功');
    await expect(page.locator('.asset-list')).toContainText('测试资产');
  });
});
```

---

## 5. 测试执行计划

### 5.1 测试执行时间表

| 周次 | 阶段 | test-engineer-unit | test-engineer-integration | test-engineer-e2e |
|------|------|-------------------|---------------------------|-------------------|
| 第1周 | 测试准备 | 搭建测试框架 | 准备测试环境 | 搭建E2E框架 |
| 第2周 | P0服务测试 | 认证/血缘/质量 | 数据库/缓存/消息 | 核心流程测试 |
| 第3周 | P1服务测试 | 数据发现/知识图谱 | 服务间集成 | 业务流程测试 |
| 第4周 | P2服务测试 | 支撑服务 | 全链路集成 | 回归测试 |
| 第5周 | 测试收尾 | 覆盖率优化 | 缺陷修复验证 | 验收测试 |

### 5.2 测试里程碑

| 里程碑 | 完成时间 | 交付物 |
|--------|----------|--------|
| M1: 单元测试完成 | 第3周末 | 单元测试报告，覆盖率报告 |
| M2: 集成测试完成 | 第4周末 | 集成测试报告 |
| M3: E2E测试完成 | 第5周末 | E2E测试报告 |
| M4: 测试总结 | 第6周 | 综合测试报告，验收报告 |

---

## 6. 测试报告模板

### 6.1 测试执行报告

```markdown
# 测试执行报告

## 基本信息
- 项目名称：企业数据资产管理系统
- 版本：V1.0.0
- 测试时间：2026-04-10 至 2026-04-30
- 测试人员：测试团队

## 测试结果汇总

| 测试类型 | 用例数 | 通过 | 失败 | 通过率 |
|----------|--------|------|------|--------|
| 单元测试 | X | X | X | XX% |
| 集成测试 | X | X | X | XX% |
| E2E测试 | X | X | X | XX% |

## 代码覆盖率

| 模块 | 行覆盖率 | 分支覆盖率 | 方法覆盖率 |
|------|----------|------------|------------|
| edams-auth | XX% | XX% | XX% |
| lineage-service | XX% | XX% | XX% |
| quality-service | XX% | XX% | XX% |
| ... | ... | ... | ... |

## 缺陷统计

| 严重程度 | 数量 | 已修复 | 遗留 |
|----------|------|--------|------|
| 严重 | X | X | X |
| 高 | X | X | X |
| 中 | X | X | X |
| 低 | X | X | X |

## 测试结论
[通过/条件通过/不通过]

## 遗留问题清单
| 问题ID | 问题描述 | 严重程度 | 计划处理 |
|--------|----------|----------|----------|
| ... | ... | ... | ... |
```

---

**文档结束**
